kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: package-hbase-client
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: sapujagad2-package-hbase-client
